# FastAPI and Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10
greenlet>=3.0.0

# Caching and Background Jobs
redis==5.2.1
celery==5.4.0

# Data Validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# HTTP Client
httpx==0.28.1
aiohttp==3.11.11

# Web Scraping (Runtime only if needed, minimal)
beautifulsoup4==4.12.3
lxml==5.3.0
# scrapy removed (too big)

# Search
# elasticsearch removed (unused)

# Data Processing
# pandas removed (too big)
# numpy removed (too big)

# Testing
# pytest removed (dev only)
# pytest-asyncio removed (dev only)
